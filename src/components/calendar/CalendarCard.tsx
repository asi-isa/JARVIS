import Card from "../dashboard/card";

interface CalendarCardProps {}

const CalendarCard = ({}: CalendarCardProps) => {
  return (
    <Card>
      <div>
        <p>Calendar card</p>
      </div>
    </Card>
  );
};

export default CalendarCard;
