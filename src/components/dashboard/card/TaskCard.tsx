import Card from ".";

interface TaskCardProps {}

const TaskCard = ({}: TaskCardProps) => {
  return (
    <Card>
      <div>
        <p>Tasks</p>
      </div>
    </Card>
  );
};

export default TaskCard;
